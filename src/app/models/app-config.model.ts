export interface AppConfig {
  apiPath?: string;
  apiPort?: number;
  apiProtocol?: string;
  apiServer?: string;
  applicationProtocol?: string;
  applicationServer?: string;
  applicationUrl?: string;
  envName?: string;
  production?: boolean;
  musicApiPrefix: string;
}